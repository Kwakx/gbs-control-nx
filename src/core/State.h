#ifndef STATE_H
#define STATE_H

#include <Arduino.h>
#include "options.h"

// State.h contains application state data structures
// Structures are defined in options.h, this file serves as a central access point

// Forward declarations for types from options.h
// (structures are already defined in options.h)

#endif // STATE_H

